import React from 'react';
import WineDetailPage from './WineDetailPage';

const RoseWineDetail = ({ roseWine }) => {
  return (
    <WineDetailPage wine={roseWine} />
  );
};

export default RoseWineDetail;
